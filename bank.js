var balance = +prompt("Enter your balance "); //+prompt is to convert string to number
var damount = +prompt("Enter your damount");  //+prompt is to convert string to number
var newbalance = (balance + damount);
console.log(newbalance);
